// Create two string variables called name and greeting. 
// Use console.log to output both variables separated by a string using string concatenation (use the + operator). 
// Then use console.log to output both variables separated by a string using string interpolation (use the `${var1} ${var2}` syntax)

let name = "Marcel";
let greeting = "Hi ";

let name1 = "Marcel";
let greeting1 = "Hi";

console.log(greeting + name);
console.log(`${greeting1} ${name1}`);